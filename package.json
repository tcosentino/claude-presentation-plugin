{
  "name": "claude-presentation-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "A Claude Code plugin that generates HTML slide-deck presentations with audio narration",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^3.1.0"
  }
}
